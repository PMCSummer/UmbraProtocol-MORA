# T04 seam

phase: T04
direct_upstream:
- T03 — Hypothesis competition / silent convergence
- C04 — Endogenous mode arbitration
- C05 — Temporal validity / selective revalidation
- T01 — Semantic field / active non-verbal scene
- T02 — Relation binding / constraint propagation
direct_downstream:
- O01 — User / Other entity model
- V01 — (future downstream)
- V02 — (future downstream)
- N01 — (future downstream)
- N02 — (future downstream)
- D01 — (future downstream)

## OPERATIONAL UPSTREAM
- T03 — primary operational dependency for competition/frontier output
- C04/C05 — mode/validity claims consumed as bounded control/owner inputs
- T01/T02 — scene + constrained structure are upstream context, not reimplemented here

## OPERATIONAL DOWNSTREAM
- future downstream consumers should receive typed attention schema contract only
- this seam does not implement O01/V*/N*/D* behavior

## OPEN SEAMS TO LEAVE
- downstream interpretation phases (O01, V01, V02, N01, N02, D01) remain open
- global workspace / planner / action selection remain outside this seam

## FORBIDDEN OVERREACH
- collapsing T03 frontrunner/salience into full attention ownership by default
- exporting polished focus label without ownership/provenance/control surfaces
- silently erasing peripheral/mixed targets under unresolved competition
- claiming final closure / final action authority / world-truth authority

## SEAM OBLIGATIONS
- materialize typed T04 attention schema state with:
  - `focus_targets`
  - `peripheral_targets`
  - `attention_owner`
  - `focus_mode`
  - `control_estimate`
  - `stability_estimate`
  - `redirect_cost`
  - `reportability_status`
  - `provenance`
- keep ownership distinct from local prominence/salience
- preserve mixed/provisional/peripheral targets when lawful basis is weak
- expose reportability separately from stability
- preserve first-slice bounded scope marker truth in public contract surfaces

## LOAD-BEARING SEAM TELEMETRY
- `rt01.t04_attention_schema_checkpoint` status/reason/required_action
- focus-owner/readiness + peripheral-preservation readiness
- forbidden T04 shortcut markers:
  - `highest_salience_as_attention_schema`
  - `ownership_dropped_from_export`
  - `peripheral_uncertainty_collapsed`
  - `reportability_overstated_when_unstable`

## REQUIRED SEAM TESTS
- typed T04 schema state materializes
- ownership/control contrast differs under same T03 leader when C04/C05 conditions differ
- peripheral/mixed targets survive unresolved/competing conditions
- high salience does not auto-promote to owned focus
- reportability does not masquerade as stable when stability is weak
- RT01-local consumer requirements are path-affecting when requested

## SEAM FALSIFIERS
- owner collapses to salience ranking only
- peripheral targets vanish on switch/competition pressure
- reportability exposed as stable while stability remains weak
- checkpoint present only in telemetry without route/outcome effect

