# F01 seam

phase: F01
direct_upstream:
- none
direct_downstream:
- F02 — Grounding / epistemic substrate
- R01 — Vital / allostatic foundation
- R02 — Internal self-regulation affordances

## UPSTREAM
- direct upstream phases:
  - none
- допустимо опираться на:
  - собственный typed runtime-state contract и authority matrix
  - closure artifacts F01 (ADR + validation tests)
- запрещено предполагать:
  - semantic/epistemic/regulatory outputs будущих фаз
  - любые write paths вне execute_transition(...)

## DOWNSTREAM
- direct downstream phases:
  - F02 — Grounding / epistemic substrate
  - R01 — Vital / allostatic foundation
  - R02 — Internal self-regulation affordances
- downstream должен получить:
  - canonical transition artifacts: TransitionResult, StateDelta, ProvenanceRecord
  - honest failure surface с authority decision и reject semantics
- downstream не должен получать:
  - eventless state changes
  - write side effects без provenance/authority

## OPEN SEAMS TO LEAVE
- grounding/semantics (F02+) не закрываются в causal substrate
- regulation/policy/planning/dialogue логика остаётся downstream

## FORBIDDEN OVERREACH
- интерпретация содержания, world-truth claims, intent/planning decisions
- второй mutation gateway кроме execute_transition(...)

## SEAM OBLIGATIONS
- сохранять единственный canonical write seam execute_transition(...)
- записывать attempted_paths + actual_delta + authority + failure в persisted trace

## LOAD-BEARING SEAM TELEMETRY
- runtime.revision, runtime.last_transition_id, runtime.last_event_id
- trace.events + trace.transitions(ProvenanceRecord: attempted_paths, actual_delta, authority, failure_code)

## REQUIRED SEAM TESTS
- invalid authority -> rejected transition without silent mutation
- provenance/delta persistence reconstructable from runtime-state trace
- bypass mutation blocked on public operational surface

## SEAM FALSIFIERS
- direct mutation path exists outside execute_transition(...)
- accepted/rejected transition can be emitted without provenance or delta
