# Phase Capsule: T01

## Read-first files
- `docs/seams/T01.seam.md`
- `src/substrate/t01_semantic_field/__init__.py`
- `src/substrate/subject_tick/update.py` (T01 checkpoint consumer)
- `src/substrate/runtime_topology/policy.py` (T01 node/order/checkpoint)

## Phase purpose
- Typed active semantic field / active non-verbal scene, consumed pre-verbally before wording.

## Authority boundary
- T01 is a semantic field contract surface; not action selection, not final closure.

## Anti-creep boundary
- Do not implement downstream `T02/T03/O01` logic inside T01.
- Do not collapse to hidden text / bag-of-tags / label-only telemetry.

## Direct upstream (seam)
- `A03`, `C01`, `C02`, `C05`, `G02`, `G03`, `G07`, `L06`, `M03`, `S05`.

## Direct downstream (seam)
- `O01`, `T02`, `T03`.

## Must-read raw files only if changing internals
- `src/substrate/t01_semantic_field/models.py`
- `src/substrate/t01_semantic_field/policy.py`
- `src/substrate/t01_semantic_field/downstream_contract.py`
- `src/substrate/t01_semantic_field/telemetry.py`

## Checkpoints touching this phase
- `rt01.t01_semantic_field_checkpoint` (source contract: `t01_semantic_field.active_non_verbal_scene`)

## Consumer flags
- `require_t01_preverbal_scene_consumer`
- `require_t01_scene_comparison_consumer`

## First test ladder
1. `tests/substrate/test_subject_tick_build/test_subject_tick_build.py` (`test_subject_tick_t01_*`)
2. `tests/substrate/test_runtime_topology_build/test_runtime_topology_build.py` (`test_dispatch_t01_*`)
3. `tests/substrate/test_t03_hypothesis_competition_build/test_t03_hypothesis_competition_build.py` (regression boundary where T01 feeds T03)

## Forbidden broadening
- No T02/T03 behavior migration into T01.
- No claim of full silent-thought line from T01-only surfaces.
